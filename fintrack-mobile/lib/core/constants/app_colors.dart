// lib/core/constants/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0F766E); // Deep teal
  static const Color secondary = Color(0xFF0EA5E9); // Bright sky blue
  static const Color background = Color(0xFFF8FAFC); // Off-white / Slate 50
  static const Color surface = Color(0xFFFFFFFF); // Pure white
  static const Color textPrimary = Color(0xFF0F172A); // Dark slate / Slate 900
  static const Color textSecondary = Color(0xFF64748B); // Slate gray / Slate 500
  static const Color border = Color(0xFFE2E8F0); // Light border / Slate 200
  static const Color accent = Color(0xFF22C55E); // vivid green for the + button
  static const Color error = Color(0xFFEF4444);  // red 500, used for over-budget states
}
